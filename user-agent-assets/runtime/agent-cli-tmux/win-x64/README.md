Windows runtime payload placeholder.

Phase 4 では `AgentCliTmux.exe` の publish 済み payload ではなく、install 構造と wrapper 導線を先に確立する。
Windows 実 payload は follow-up 実装で `AgentCliTmux.exe` を配置するか、wrapper から publish 済み配置を参照する。